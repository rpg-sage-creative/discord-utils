import { debug, info, warn, error } from "@rsc-utils/core-utils";
import { assert, runTests, startAsserting, stopAsserting } from "@rsc-utils/core-utils";

runTests(async function testOthers() {
	error("Create mention tests!");
	error("Create parseIds tests!");
	error("Create url tests!");
	error("Create DiscordKey tests!");
	error("Create errorHandler tests!");
	error("Create toHumanReadable tests!");
}, true);
